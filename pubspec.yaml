name: my_bio_app
description: A multi-platform autobiography app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✔ 推薦版本：跨平台完全無錯
  file_picker: ^8.0.0

  # ✔ image_picker 最新穩定版（修正 Windows / Web 支援）
  image_picker: ^1.0.8

  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/profile.jpg
